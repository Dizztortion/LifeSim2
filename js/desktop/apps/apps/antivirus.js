(function () {
  var UI = (window.UI = window.UI || {});
  if (!UI.PCApps || !UI.PCApps.register) return;
  UI.PCApps.register({ id: "antivirus", title: "AntiVirus", sub: "Security", emoji: "🛡️", window: { w: 640, h: 520 } });
})();

