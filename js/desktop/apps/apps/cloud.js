(function () {
  var UI = (window.UI = window.UI || {});
  if (!UI.PCApps || !UI.PCApps.register) return;
  UI.PCApps.register({ id: "cloud", title: "Cloud Mining", sub: "Contracts", emoji: "☁️", window: { w: 700, h: 540 } });
})();

