(function () {
  var UI = (window.UI = window.UI || {});
  if (!UI.PCApps || !UI.PCApps.register) return;
  UI.PCApps.register({ id: "mining", title: "Mining Rig", sub: "Hardware", emoji: "⛏️", window: { w: 720, h: 560 } });
})();

