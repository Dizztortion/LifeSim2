(function () {
  var UI = (window.UI = window.UI || {});
  if (!UI.PCApps || !UI.PCApps.register) return;
  UI.PCApps.register({ id: "tools", title: "Tools", sub: "Utilities", emoji: "🧰", window: { w: 660, h: 540 } });
})();

